{
  "name": "zitnr-utils",
  "version": "1.0.16",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest",
    "prettier": "prettier --write '**/*.ts'",
    "pub": "yarn test && yarn prettier && yarn tsc && yarn publish"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "prettier": "^2.8.8",
    "ts-jest": "^29.1.0",
    "typescript": "^5.8.2"
  }
}
